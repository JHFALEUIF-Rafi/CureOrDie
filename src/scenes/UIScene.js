class UIScene extends Phaser.Scene {
    constructor() {
        super({ key: 'UIScene' });
    }

    create() {
        // Escena para superponer UI del juego
        // Se usará cuando el GameScene esté activo
    }
}
